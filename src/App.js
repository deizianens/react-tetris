import React from 'react';
import PropTypes from 'prop-types';

import './App.css';
import Tetris from './components/Tetris';

const App = (props) => {
  return <Tetris />;
};

App.propTypes = {};

export default App;
